extendedbackoffice.application-context=extendedbackoffice-spring.xml

# Amount of days the credentials stay valid after last password change (values <= 0 will deactivate the feature)
employee.credentials.expiration.days=0